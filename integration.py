'''
  inputs: (f, a, b, n)
  f => function to be integrated or table with n values f(x_i) i=1 to n sampled uniformly in [a, b].
  a => start of integration interval,
  b => end of integration interval,
  n => number of sample points
'''
